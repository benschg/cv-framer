{
  "name": "benjamin-grauer-monorepo",
  "private": true,
  "workspaces": [
    "cv-builder"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:cv-builder": "cd cv-builder && bun dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.3.3"
  }
}
